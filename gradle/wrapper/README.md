# Gradle Wrapper

The Gradle wrapper JAR file should be downloaded automatically when you run `./gradlew` for the first time.

If you need to manually install the wrapper, run:
```bash
gradle wrapper --gradle-version 8.2
```

This will download the required gradle-wrapper.jar file to the gradle/wrapper directory.
